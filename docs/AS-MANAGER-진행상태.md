# AS Manager — 진행 상태 요약 (2026년 4월 5일 기준)

## 배포 정보
- **Vercel URL**: https://as-manager-murex.vercel.app
- **GitHub**: as-manager 저장소 (git push → Vercel 자동 배포)
- **로그인**: admin@asmanager.com / 21992199
- **Supabase**: 프로젝트 ID dlzasdhhwgxshkyuzfyp (서울)
- **httpSMS**: 연동 완료, API 키 uk_로 시작, 발신번호 010-6779-0989
- **Webhook**: https://as-manager-murex.vercel.app/api/sms/webhook (httpsms.com에 등록됨)

## 완료된 작업 (전체)

### 1단계: GitHub + Vercel 배포 ✅
### 2단계: AS 일지 ✅
- KPI 다크바 버튼 (전체/접수/수리중/완료/불가/삭제)
- 필터 라벨 (구분/브랜드/상태/기간)
- 4색 컬럼 그룹 (입고/AS처리/입금/출고)
- 인라인 편집 (뱃지 펼침 방식, 즉시 저장)
- 검색: 2글자 이상 입력 시 전체 기간 검색 (debounce 300ms)
- 검색 드롭다운 (고객 목록, 클릭→팝업)
- 거래처/성함 뱃지 + 문자 아이콘 컬럼
- 모델명 파란 뱃지 (#E6F1FB/#0C447C)
- 빈 셀: 파란 동그라미 ● (—금지)
- 날짜 정렬: intake_date 내림차순 기본
- 컬럼 리사이즈 (드래그)
- 삭제 모드 토글
- 택배 버튼: 완료+입금완료+미출고 시 표시 → 택배발송에 자동 등록
- 출고 영역: 읽기 전용 (택배발송에서 자동 입력)
- 새 접수 행: 엔터 키 데이터 유지
- 새로고침 시 현재 탭 유지 (localStorage)

### 3단계: 고객 이력 팝업 ✅
- 거래처/성함 또는 문자 아이콘 클릭 → 팝업
- 헤더: 고객명+연락처+총AS+총비용+보증중
- 좌측: 수리이력 (전체 기간, 보증뱃지)
- 우측: 문자채팅 (발신파란/수신회색) + 입력창
- 크기: 850px, 75vh, resize: both

### 4단계: 택배발송 ✅
- 12개 컬럼: 날짜(자동)/수령자명(자동)/HP(자동)/주소(수동)/품목명(자동)/수량(수동)/배송메시지(수동)/선불착불(뱃지)/출고처(AS고정)/택배사(뱃지)/운송장번호(수동)/삭제
- 수령자명 자동완성 (미발송 고객 드롭다운)
- 송장번호 입력 → AS일지 출고 자동 업데이트
- 삭제 → AS일지 출고 초기화
- 송장 엑셀 출력
- 날짜 정렬: ship_date 내림차순

### 5단계: 수리내역조회 → AS일지 통합 ✅
- 별도 탭 삭제, 고객 팝업으로 통합

### 6단계: 부속가격 ✅
- 93개 부품 데이터 표시
- 검색/필터 (부품코드/품명/스펙)
- 구분 드롭다운 필터
- 수정/추가 모달 (이미지 업로드, Supabase Storage)
- 컬럼 리사이즈

### 7단계: 설정 ✅
- 정산관리: 비밀번호 잠금(1234), 월별 KPI 4개, 정산 테이블+월이동
- 시스템설정: 문자 템플릿+변수삽입, SMS 연동, 처리자 CRUD, 보증기간, 비밀번호 변경
- 2컬럼 가로 배치

### 8단계: httpSMS 연동 ✅
- 문자 발송: /api/sms/send (서버사이드)
- 문자 수신: /api/sms/webhook
- 테스트 발송 성공
- 웹훅 설정 완료

### 추가 완료
- 탑 네비: B타입 배경 하이라이트, 15px 텍스트
- 문자 알림 버튼 + 채팅 팝업 (고객목록+채팅)
- 뱃지 펼침 드롭다운 (select 태그 완전 교체)

## 내일 작업 예정

| 우선순위 | 내용 | 상태 |
|---------|------|------|
| 🔴 높음 | 자동 문자 발송 (입고/출고 시 템플릿 자동 발송) | ⬜ |
| 🟡 중간 | 대시보드 (월별 통계 차트) | ⬜ |
| 🟡 중간 | 고객 DB (고객 정보 별도 관리) | ⬜ |
| 🟢 낮음 | 모바일 반응형 | ⬜ |
| 🟢 낮음 | AS 일지 엑셀 내보내기 양식 개선 | ⬜ |
| 🟢 낮음 | UI 미세 조정 (사용하면서) | ⬜ |

## 내일 새 채팅 시작 방법
1. 이 프로젝트에서 새 채팅 열기
2. "AS Manager 이어서 작업하자" 라고 말하면 됨
3. 메모리에 프로젝트 정보 저장되어 있음
